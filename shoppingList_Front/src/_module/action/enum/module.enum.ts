export enum ModuleEnum {
  ACCOUNT = 'account',
  ALERT = 'alert',
  CATEGORISATION = 'categorisation',
  ENUM = 'enum',
  FORM = 'form',
  LAYOUT = 'layout',
  LOADER = 'loader',
  PRODUCT = 'product',
  SHOPPING_LIST = 'shopping-list',
  TIMER = 'timer',
  TOKEN = 'token',
}
